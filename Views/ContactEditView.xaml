<UserControl x:Class="PhoneBook.Views.ContactEditView"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
mc:Ignorable="d"
d:DesignHeight="300" d:DesignWidth="400">

    <StackPanel Margin="20">
        <TextBlock Text="Редактирование контакта" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <TextBlock Text="Имя:" Margin="0,5"/>
        <TextBox Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

        <TextBlock Text="Телефон:" Margin="0,10,0,5"/>
        <TextBox Text="{Binding EditPhone, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

        <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Margin="5" Width="100"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}" Margin="5" Width="100"/>
        </StackPanel>
    </StackPanel>
</UserControl>

